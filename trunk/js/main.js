function hideButtons(){
document.getElementById('selectuser_submit').style.display='none'; // hides button if JS is enabled
document.getElementById('mycasecount_submit').style.display='none'; // hides button if JS is enabled
document.getElementById('dateselection_submit').style.display='none'; // hides button if JS is enabled
document.getElementById('showdetails_submit').style.display='none'; // hides button if JS is enabled
document.getElementById('schedule_submit').style.display='none'; // hides button if JS is enabled
return 1;
}
